{% comment %}
  SVG Screen Configurator
  Dynamic SVG representation matching Figma node 151:891.

  Structure:
  - Cassette: 64px height, 10px radius on top corners only
  - Rails: 26px width left/right
  - Fabric: Fills the center area
  - Bottom Plate: 16px height

  Default: 1500mm x 1000mm at 0.3 scale = 450 x 300 visual units

  The JavaScript (svg-screen-configurator.js) re-renders this dynamically.
{% endcomment %}

<div
  id="svg-screen-configurator"
  class="svg-screen-configurator"
  data-frame-color="anthracite"
  data-fabric-color="charcoal"
  data-fabric-type="4-percent"
  data-width="1500"
  data-height="1000"
  aria-label="Interactive screen configurator preview"
>
  <svg
    viewBox="0 0 490 340"
    preserveAspectRatio="xMidYMid meet"
    class="configurator-svg"
    role="img"
    aria-label="Screen preview showing current configuration"
  >
    <!-- Default static render: 1500x1000mm = 450x300 units -->
    <!-- Padding 20 on all sides -->
    <g transform="translate(20, 20)">

      <!-- CASSETTE: 450w x 64h, top corners rounded 10px -->
      <rect x="0" y="0" width="450" height="64" rx="10" ry="10" fill="#454545" />
      <!-- Square off bottom corners -->
      <rect x="0" y="54" width="450" height="10" fill="#454545" />

      <!-- RAILS AND FABRIC: starts at y=64 -->
      <g transform="translate(0, 64)">

        <!-- Rails height: 300 - 64 = 236 -->

        <!-- LEFT RAIL: 26w -->
        <rect x="0" y="0" width="26" height="236" fill="#454545" />

        <!-- RIGHT RAIL: at x=424, 26w -->
        <rect x="424" y="0" width="26" height="236" fill="#454545" />

        <!-- FABRIC: 398w, 220h (236 - 16 bottom plate) -->
        <rect
          x="26"
          y="0"
          width="398"
          height="220"
          fill="#707070"
          opacity="0.92"
          class="fabric-rect"
        />

        <!-- BOTTOM PLATE: 398w, 16h -->
        <rect x="26" y="220" width="398" height="16" fill="#454545" />

      </g>
    </g>
  </svg>

  <!-- Dimension Labels Overlay -->
  <div class="dimension-labels">
    <span class="dimension-label dimension-width">
      <span class="dimension-value" data-dimension="width">1500</span> mm
    </span>
    <span class="dimension-label dimension-height">
      <span class="dimension-value" data-dimension="height">1000</span> mm
    </span>
  </div>
</div>
